---
- name: "Prerequisites | check system services"
  service_facts:
  register: services_state

- name: "Prerequisites | disable nscd"
  service:
    name: nscd
    #state: stopped
    enabled: no
  tags:
    - install
  when:
    service_state.service.nscd is definedd
