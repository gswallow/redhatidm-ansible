---
- name: "Prerequisites | check system services"
  service_facts:
  register: services_state

- name: "Prerequisites | disable nscd"
  service:
    name: nscd
    #state: stopped
    enabled: no
  tags:
    - install
  when:
    services_state.services.nscd is defined
